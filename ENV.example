# AI Gen Studio 环境变量配置示例
# 请复制此文件为 wrangler.jsonc 并填入实际值

# NextAuth 配置（必须）
NEXTAUTH_SECRET=generate-a-long-random-secret
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth（在 https://github.com/settings/developers 创建 OAuth App）
GITHUB_ID=
GITHUB_SECRET=

# Google OAuth（在 https://console.cloud.google.com/apis/credentials 创建 OAuth 客户端）
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Replicate API 令牌（用于 FLUX 图像生成）
REPLICATE_API_TOKEN=your-replicate-api-token-here

# Cloudflare 服务配置
# 这些值需要在 Cloudflare Dashboard 中创建对应服务后获取

# D1 数据库 ID
DATABASE_ID=your-database-id-here

# KV 命名空间 ID  
KV_NAMESPACE_ID=your-kv-namespace-id-here

# Cloudflare R2 存储配置（替换 Supabase Storage）
CLOUDFLARE_R2_ACCOUNT_ID=your-r2-account-id
CLOUDFLARE_R2_ACCESS_KEY_ID=your-r2-access-key-id
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
CLOUDFLARE_R2_BUCKET_NAME=your-r2-bucket-name
CLOUDFLARE_R2_PUBLIC_URL=https://your-bucket.r2.dev  # 可选，如果配置了自定义域名

# Cloudflare Images 配置（用于图片优化和转换）
CLOUDFLARE_IMAGES_ACCOUNT_HASH=your-account-hash-here  # 从 Cloudflare Dashboard → Images 获取
CLOUDFLARE_IMAGES_API_TOKEN=your-api-token-here  # 可选，如果使用 API 上传图片
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id  # 可选，如果使用 API 上传

# Stripe 支付配置
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...  # 可选，用于 webhook 验证

# RunningHub API 配置
RUNNINGHUB_API_KEY=your-runninghub-api-key
RUNNINGHUB_BASE_URL=https://api.runninghub.com  # 替换为实际的 RunningHub API URL 

# Supabase Public Keys (Browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase Service Key (Server-only, e.g., seed script)
SUPABASE_SERVICE_ROLE_KEY=
# NOTE: To sync auth users, server writes will upsert into `users` table using this key.

# （可选）站点基础 URL，用于 SEO（如需要覆盖内置域名时）
NEXT_PUBLIC_SITE_URL=

# （可选）Supabase Postgres 直连/Prisma 所需变量
# 如未使用 Prisma 或数据库直连，可忽略本段
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_PRISMA_URL=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=
POSTGRES_HOST=

# （可选）若启用 Supabase Auth 或 Edge Functions 相关策略时可能需要
SUPABASE_JWT_SECRET=